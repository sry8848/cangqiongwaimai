<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">

    <!-- 根据订单id查询订单详情 -->
    <select id="listByOrderId" resultType="com.sky.entity.OrderDetail">
        select * from order_detail where order_id = #{orderId}
    </select>

    <!-- 批量插入订单详情 -->
    <insert id="insertBatch" parameterType="orderDetail">
        insert into order_detail (order_id, dish_id, setmeal_id, name, image, dish_flavor, number, amount)
        values
        <foreach collection="list" item="orderDetail" separator=",">
            (#{orderDetail.orderId}, #{orderDetail.dishId}, #{orderDetail.setmealId}, #{orderDetail.name}, 
             #{orderDetail.image}, #{orderDetail.dishFlavor}, #{orderDetail.number}, #{orderDetail.amount})
        </foreach>
    </insert>

    <!-- 根据订单id删除订单详情 -->
    <delete id="deleteByOrderId">
        delete from order_detail where order_id = #{orderId}
    </delete>

</mapper>
